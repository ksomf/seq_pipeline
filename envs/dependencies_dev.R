install.packages(c( 'ggplot2', 'ggthemes', 'tidyverse', 'BiocManager' ))
BiocManager::install(c( 'GenomicRanges', 'Rsamtools' )) #NOTE(KIM): Might need to be run more than once. Yay software quality!
